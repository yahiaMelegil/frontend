# User Authentication Package

- Branch: `feature/user-auth`
- Owner: Raghad
- Feature routes: `/login`, `/register`, `/forgot-password`, `/reset-password`, `/verify-email`, `/verify-email/:id/:hash`, and `/email-verified`.
- Feature pages: login, register, forgot password, reset password, email verification, and email verified.
- Shared infrastructure: the single `AuthProvider`, `useAuth`, route guards, token manager, API client, authentication UI primitives, and user authentication service.
- Excluded UI features: landing page, expert authentication pages, admin authentication pages, admin dashboard pages, expert dashboard pages, and user dashboard pages.

